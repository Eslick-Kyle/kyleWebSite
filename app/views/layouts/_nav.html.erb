<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Kyle Eslick" , root_path, :class => 'navbar-brand' %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <%= nav_link "About", about_index_path %>
        <%= nav_link "Coding", code_index_path %>
        <%= nav_link "Photography", photography_index_path %>
        <%= nav_link "Contact", contact_index_path %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <%= nav_link current_user.email, edit_user_registration_path %>
        <li><%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></li>
        <% else %>
        <%= nav_link "Sign up", new_user_registration_path %>
        <%= nav_link "Login", new_user_session_path %>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
